upstream bidpilot_api_backend {
    server 127.0.0.1:3101;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name bidpilot-api.rglens.com;

    location ^~ /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    listen [::]:443 ssl;
    server_name bidpilot-api.rglens.com;

    ssl_certificate /usr/local/openresty/nginx/conf/ssl/bidpilot-api.rglens.com/fullchain.pem;
    ssl_certificate_key /usr/local/openresty/nginx/conf/ssl/bidpilot-api.rglens.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    client_max_body_size 100m;

    # The API owns the Assistant SSE stream. Do not buffer or cache it, and
    # keep the upstream HTTP/1.1 connection reusable for ordinary API calls.
    location / {
        proxy_pass http://bidpilot_api_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
